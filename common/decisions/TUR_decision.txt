TUR_debug_decision = {
	TUR_decision_01= {
		activation = {
			original_tag = TUR
		}
		available = {
			date > 1937.2.1
		}
		is_good = yes
		fired_only_once = yes
		days_mission_timeout = 5
		complete_effect = {
			
		}
		timeout_effect = {
			declare_war_on = { target = GRE type = annex_everything } 
			country_event = TUR_event.1
			news_event = TUR_news_event.1
		}
	}
}
